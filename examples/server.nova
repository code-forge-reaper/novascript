Runtime.panic("this should get something in the future")
@route("/") def()
	return "hi"
end

@route("/%") def (filePath string)
	return Fs.read(filePath, {mode: "r"})
end
@route("/%/%") def (dirName, filePath string)
	return Fs.read(Fs.join(dirName, filePath), {mode: "r"})
end
